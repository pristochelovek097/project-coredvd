; firmware.asm - прошивка для dvd-процессора pulse
; ассемблируется в firmware.bin (13 байт)
; инструкции процессора:
;   MOV reg, imm32  - опкод 0x01, 1 байт регистр, 4 байта imm32 (little-endian)
;   LOAD_FRAME      - опкод 0x03, без аргументов, зацикливает выполнение
; есть и больше в документации

; инициализация
    mov  r4, 0x00000000   ; счётчик кадров = 0 (будет использоваться для OSD и глав)
    mov  r0, 0x00000000   ; указатель на VOB-данные = 0 (начало видео)

; главный цикл воспроизведения
loop:
    load_frame             ; декодировать кадр, обновить экран, сдвинуть VOB‑позицию
    ; LOAD_FRAME автоматически возвращает PC на себя, поэтому здесь бесконечный цикл
